<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:drawing="clr-namespace:SnippingToolWPF.Drawing" xmlns:local="clr-namespace:SnippingToolWPF">

    <DataTemplate DataType="{x:Type local:PencilsSidePanelViewModel}">
        <drawing:PencilsSidePanel />
    </DataTemplate>
    <DataTemplate DataType="{x:Type local:ShapesSidePanelViewModel}">
        <drawing:ShapesSidePanel />
    </DataTemplate>
    <DataTemplate DataType="{x:Type local:StickersSidePanelViewModel}">
        <drawing:StickersSidePanel />
    </DataTemplate>
    <DataTemplate DataType="{x:Type local:TextSidePanelViewModel}">
        <drawing:TextSidePanel />
    </DataTemplate>
</ResourceDictionary>